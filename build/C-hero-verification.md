# C Hero And Flagship Verification

- Rebuilt `components/sections/hero/Hero.tsx` and `Hero.module.css` as the first viewport: one `h1`, two-line Tanveer/LegacyLift identity, factual gap-year bio, `Explore LegacyLift` CTA to `#flagship`, and a reserved right-side `data-orb-anchor="dormant"` slot using the shared `OrbPoster`.
- Rebuilt `components/sections/Flagship.tsx` and `Flagship.module.css` as the LegacyLift act: sticky desktop explanatory sequence, `Migration needs proof.` heading, all three verification stages always present, `data-verification-step` hooks, and `data-verification-visual` on the active-thinking art slot.
- Included all requested facts from the current source: overflow truncation bug, BYOK/source isolation, 1Foundry Theme 1 deeptech, Fidelity Replay, and a substantive `/legacylift` case-study link.
- Added an explicit illustrative caption for the verification scene and kept mobile/reduced-motion as a compact natural flow with no long sticky scroll.
- Did not run builds or install dependencies, per instruction.
